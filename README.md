# Installation

Install required Python packages in `requirements.txt` (requires `cmake`).

## Constrained Signal Generation via Symbolic Timed Automata
Follow the instructions in ```symbolic_timed_automata/lib/README.md```

